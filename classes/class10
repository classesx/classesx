<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Class 10 – Subjects | Partap Classes</title>
  <style>
    body {
      font-family: 'Inter', sans-serif;
      background: radial-gradient(circle at top, #0f1624 0%, #070b12 100%);
      color:white;
      margin:0;
      padding:0;
      overflow-x:hidden;
    }

    /* Floating particles */
    .particles {
      position: fixed;
      top:0; left:0;
      width:100%; height:100%;
      pointer-events:none;
      overflow:hidden;
    }
    .particle {
      position:absolute;
      width:6px; height:6px;
      background:white;
      opacity:0.25;
      border-radius:50%;
      animation: floatUp 10s linear infinite;
    }
    @keyframes floatUp {
      from { transform: translateY(120vh); opacity:0; }
      to { transform: translateY(-20vh); opacity:0.3; }
    }

    /* Header section */
    .header-box {
      padding:60px 30px 20px;
      text-align:center;
    }
    h1 {
      font-weight:900;
      font-size:42px;
      letter-spacing:1px;
      background: linear-gradient(90deg, #ffffff, #aab7ff);
      -webkit-background-clip:text;
      -webkit-text-fill-color:transparent;
      animation: glowTitle 4s ease-in-out infinite;
    }
    @keyframes glowTitle {
      0%,100% { filter: drop-shadow(0 0 6px rgba(255,255,255,0.4)); }
      50% { filter: drop-shadow(0 0 16px rgba(255,255,255,0.9)); }
    }
    p.subtitle {
      opacity:0.7;
      font-size:18px;
      margin-top:-10px;
    }

    /* Subject grid */
    .subject-grid {
      display:grid;
      grid-template-columns:repeat(auto-fit,minmax(200px,1fr));
      gap:24px;
      padding:40px;
    }

    .subject-card {
      background:rgba(255,255,255,0.05);
      padding:30px 20px;
      border-radius:16px;
      font-size:20px;
      font-weight:700;
      text-align:center;
      backdrop-filter:blur(7px);
      cursor:pointer;
      transition:0.3s ease;
      border:1px solid rgba(255,255,255,0.08);
      position:relative;
      overflow:hidden;
      text-decoration:none;
      color:white;
      display:block;
    }

    .subject-card::before {
      content:"";
      position:absolute;
      top:0;left:-100%;
      width:100%;height:100%;
      background:linear-gradient(120deg,transparent,rgba(255,255,255,0.25),transparent);
      transition:0.6s;
    }
    .subject-card:hover::before{
      left:100%;
    }

    .subject-card:hover {
      transform:translateY(-6px) scale(1.04);
      box-shadow:0 12px 30px rgba(0,0,0,0.4);
    }

    .subject-card span.icon {
      font-size:26px;
      display:block;
      margin-bottom:6px;
    }

    body { opacity:0; animation:fadeIn 1s ease forwards; }
    @keyframes fadeIn { from{opacity:0;transform:translateY(20px);} to{opacity:1;transform:translateY(0);} }

    .back-home {
      margin-top:25px;
      display:inline-block;
      padding:10px 18px;
      border-radius:10px;
      background:linear-gradient(90deg,#4facfe,#00f2fe);
      text-decoration:none;
      color:#000;
      font-weight:700;
      transition:0.25s;
      box-shadow:0 0 12px rgba(0,255,255,0.4);
    }
    .back-home:hover { transform:scale(1.06); box-shadow:0 0 20px rgba(0,255,255,0.8); }

  </style>
</head>
<body>

  <div class="header-box">
    <h1>Class 10 – Subjects</h1>
    <p class="subtitle">Choose any subject to open its notes & study materials</p>
  </div>

  <div class="subject-grid">

    <!-- SUBJECT LINKS ADDED -->
    <a href="subjects/english.html" class="subject-card"><span class="icon">✏️</span>English</a>
    <a href="hindi.html" class="subject-card"><span class="icon">📝</span>Hindi</a>
    <a href="science.html" class="subject-card"><span class="icon">🔬</span>Science</a>
    <a href="maths.html" class="subject-card"><span class="icon">➗</span>Mathematics</a>
    <a href="sst.html" class="subject-card"><span class="icon">🌍</span>Social Science</a>
    <a href="sanskrit.html" class="subject-card"><span class="icon">📜</span>Sanskrit</a>
    <a href="computer.html" class="subject-card"><span class="icon">💻</span>Computer</a>

  </div>

  <a href="index.html" class="back-home">⬅ Back to Home</a>

</body>
</html>
